import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
})
export class AppComponent {
  // Because this is a simple application with only 1 page,
  // I created the page inside the app template
}
